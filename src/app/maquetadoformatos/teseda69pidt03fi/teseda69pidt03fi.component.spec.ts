import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Teseda69pidt03fiComponent } from './teseda69pidt03fi.component';

describe('Teseda69pidt03fiComponent', () => {
  let component: Teseda69pidt03fiComponent;
  let fixture: ComponentFixture<Teseda69pidt03fiComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Teseda69pidt03fiComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Teseda69pidt03fiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
